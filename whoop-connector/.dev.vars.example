# Copy to .dev.vars for local `wrangler dev` testing. .dev.vars is gitignored —
# never commit real values. At deploy time these become `wrangler secret put` instead.
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
